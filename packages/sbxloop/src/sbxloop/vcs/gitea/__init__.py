"""The Gitea backend (#1021): :mod:`sbxloop.vcs.gitea.ops` and the folds
it is built from. Field-verified against Gitea 1.24.7 where a docstring
says so (#1016 and the #1021 probe); everything else is Gitea's documented
API, labelled field-unverified where it is load-bearing."""
